import React from "react";

const Button = () => {
  return (
    <button
      onClick={() => {
        alert("MFE3 Button");
      }}
    >
      MFE3 Button
    </button>
  );
};

export default Button;
